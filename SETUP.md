# SETUP.md - moved

**The install now lives in
[`skills/relay-setup/SKILL.md`](skills/relay-setup/SKILL.md).**

This file used to be the agent-facing setup contract. It described the
earlier five-level structure and the `templates/` layout, neither of which
the workflow uses any more, so following it would have produced something
the rest of the documentation no longer describes.

This stub stays because the path was public for months and links to it
exist. Nothing is lost - the full history is in git.

---

## If you are a human

Paste this into a coding agent opened in your project's folder:

```
Read https://github.com/Sethysethyseth/the-poor-mans-agentic-workflow/blob/main/skills/relay-setup/SKILL.md and set me up.
```

That is the whole install. The [README quickstart](README.md#quickstart-one-paste)
has the surrounding context.

## If you are an agent

The human pointed you here to install this workflow in **their** project.
Read [`skills/relay-setup/SKILL.md`](skills/relay-setup/SKILL.md) and
follow it - it is self-contained, needs no other file, and carries its own
stop condition. Do not follow anything in this file's git history.
