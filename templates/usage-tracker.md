# Usage tracker (weekly)

Copy this file into your repo or notes - one copy per week, or one file
with the newest week on top. It is deliberately paper-simple: the budget
unit is the WINDOW, not the token, because windows are what you can
actually plan.

Why it earns its place, twice over:

- **It's the rationing instrument.** The planner seat's 5-hour window opens
  on your FIRST prompt and expires five hours later whether you used it or
  not - capacity doesn't roll over. So open windows deliberately: a cheap
  "anchor" prompt at a chosen time puts the reset where your life needs it.
  Planning windows is the whole discipline; this file is where the plan
  lives. (Full meter mechanics: `docs/economics.md`.)
- **It's the level instrument.** Your own four weeks of rows answer the
  only question that matters: which level do you actually need? The honest
  reads are at the bottom, including the one where the answer is "buy Max."

At Level 2 this same file is the trial's mini-receipts sheet - the
`units` / `bounces` / `review catches` columns are the evidence you'll use
at the trial-end decision point (`checklists/trial-playbook.md`). One
tracker, two jobs; don't keep two.

---

## Week of <date>

### Window plan (before the week starts)

Which window-blocks do I intend to open, and when? Batch work into planned
blocks - trickling prompts across the day burns windows for nothing.

| Day | Anchor time | Intended use |
| --- | --- | --- |
| <Mon> | <e.g. 4pm - cheap anchor prompt> | <evening planner session rides this window> |
| | | |

Weekly reset lands: `<day/time - learn yours and schedule the heaviest
wave right after it>`

### Session log (one row per session, filled as you go)

| Date | Anchor | Seat/session | Units shipped | Bounces | Review catches | Cap hit? | Note |
| --- | --- | --- | --- | --- | --- | --- | --- |
| | | planner / reviewer / executor | | | | y/n | |

### Weekly review (five minutes, end of week)

- Windows used vs planned: `__ / __`
- Units landed per window: `__`
- Executor $ used of monthly allowance: `$__ / $20` *(Levels 2-4; from the
  executor's usage dashboard)*
- Cap hits this week: `__` (which seat, doing what?)
- One sentence - what would I route differently next week?

---

## Reading the tracker (the level instrument)

After 2-4 honest weeks:

- **Never hitting caps, windows half-used?** Level 1 suffices. Don't pay
  for headroom you can't use - that's the same mistake at $40 that this
  workflow calls out at $200.
- **The single meter pinching while finished blocks queue up waiting for
  tokens?** That's the exact pain the executor trial removes - Level 2 will
  pay for itself. Take the tracker's numbers into the trial as your
  baseline.
- **Cap hits weekly even on the full $40 stack, with the `MODEL:` header
  already routing mechanical work to cheap tiers?** The honest answer is a
  Max seat. This workflow's claim was never "Max is bad" - it's that most
  beginners can't use Max's headroom. Your rows just showed you're not
  most beginners.

Where to read the meters in-product (as of July 2026 - names and readouts
change; the paper tracker is the durable part): the planner seat's
`/usage` readout; the executor's dashboard usage meter; and community
tools that parse local session logs (e.g. `ccusage`) if you want
per-window detail.
